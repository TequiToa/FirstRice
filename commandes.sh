sudo apt update && sudo apt upgrade -y

apt install curl zsh kitty neofetch
curl https://sh.rustup.rs -sSf | sh                                                     #Rust + Cargo

curl -L https://sw.kovidgoyal.net/kitty/installer.sh | sh /dev/stdin                    #kitty term
sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"    #ohmyzsh

cargo install 
